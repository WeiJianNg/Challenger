import React from "react";

import Header from "../Header/Header"

const Login = () => {
  return (
    <div>
      <Header isLoginPage={true} />
      <h1>Login Page</h1>
    </div>
  );
};

export default Login;